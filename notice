

注意：
    如果更换了OS，请删除Github上的SSH key并且重新生产新的密钥。

关联远程库：
    git remote add origin git@server-name:path/repo-name.git
        初次关联则执行
            git push -u origin master 推送
        否则执行
            git push origin master 推送
